CHANGES
=======

0.2.5
-----

* feat: **拉起 WDA 按 iOS 版本分流**（新模块 `wdap/wda_launch.py`）。
  iOS 16 及以下走 `tidevice xctest`，iOS 17 及以上（含 26）走 `go-ios runwda`，
  版本读不到时先 go-ios、失败回退 tidevice。
  原因：iOS 17 起 testmanagerd 换成 RemoteXPC（RSD 隧道），tidevice 0.12.x 仍走老的
  DeveloperDiskImage 路径，会在挂载镜像阶段直接失败（`DeveloperImage not found`）。
  - 两个后端都是外部可执行程序，用 subprocess 拉起，**库里不嵌入 go-ios / tidevice**。
  - 命令契约已对照源码核对：tidevice 的 `-e` 分隔符是**冒号**（`-e USE_PORT:8100`），
    go-ios 的 `--env` 是**等号**（`--env=USE_PORT=8100`）；go-ios 的 `--bundleid` /
    `--testrunnerbundleid` / `--xctestconfig` 必须**全给或全不给**（源码硬校验）。
  - `USBClient` 新增参数 `wda_backend` / `fallback` / `tidevice_path` / `goios_path` /
    `mount_image`，并记录 `last_launch`（`WdaLaunchResult`：后端名、iOS 版本、命令行、日志路径）。
  - 新增 `mount_developer_image()`（等价 `ios image auto`），仅 go-ios 后端需要。
  - **iOS 17+ 的硬前置：go-ios tunnel daemon**。go-ios 的 `runwda` 不会自己起隧道
    （README：*For iOS 17+ devices you need to run `sudo ios tunnel start`*），
    没有隧道会在连接 testmanagerd（RemoteXPC）时失败。新增 `tunnel_agent_alive()`
    （探 `http://127.0.0.1:28100/health`）、`wait_tunnel_ready()`（探 `/ready`）、
    `ensure_tunnel()`（不在就后台拉起 `ios tunnel start [--userspace]`）；
    `start_wda(start_tunnel=)` 默认 True，`USBClient(start_tunnel=, tunnel_mode=)`。
    kernel 模式需管理员/sudo；Windows 无管理员时用 `tunnel_mode="userspace"`
    （需 wintun.dll 在 `C:/Windows/system32`）。起隧道失败不阻断，只警告。
* fix: 设备激活第二步的 Command 现在**两个名字都试**——go-ios 用
  `CreateActivationInfoRequest`，pymobiledevice3 用 `CreateTunnel1ActivationInfoRequest`，
  不同 iOS 版本认的命令不同。先试 Tunnel1，被拒再回退，两个都失败才报 `ActivationError`。
* CLI: 新增 `wda_start.py`（等价 `python -m wdap.wda_launch`），
  支持 `--mount-image` / `--tunnel-mode` / `--no-tunnel` / `--tunnel-host` / `--tunnel-port`。
* 自检: `verify_wda_launch.py` 44 项（假 .bat 可执行文件模拟成功/立即退出/找不到工具/
  回退切换，新增 8 项隧道探测与接线用例），`verify_activation.py` 16 项
  （新增命令回退与全拒用例）。

0.2.4
-----

* feat: **内建 usbmux 本地端口转发 `wdap.usbmux.UsbmuxPortForwarder`**，把
  `设备:port` 映射为 `http://127.0.0.1:<随机端口>`，纯标准库、不依赖 go-ios/tidevice。
  之所以是"根本修复"而不是再打补丁，是因为旧的 `http+usbmux` 直连通道有三个结构性缺陷：
  1. **每请求建隧道**：`http_create()` 每条 HTTP 请求都要 `select_device()`（内部
     `create_mux()` 开 2 条 usbmuxd 连接）+ `MuxDevice.connect()`（再开 2 条），
     一秒内多次请求（`is_ready` 重试、`wait_ready` 轮询、`session_id` 探测）会把
     usbmuxd（Windows 上是 Apple Mobile Device Service）的连接数推高到上限，
     之后新隧道被 RST → `WinError 10054 远程主机强迫关闭了一个现有的连接`；
  2. **无连接复用**：keep-alive 完全用不上；
  3. **无超时**：`USBMuxHTTPConnection` 不理会 `HTTPConnection.timeout`，
     而 `fetch()` 里 `conn.timeout = timeout` 又是在 socket 建好之后才赋值，等于没设。
* feat: **HTTP 连接池**。`fetch()` 现在按 `scheme://netloc` 复用连接（keep-alive），
  新增 `POOL_ENABLED` / `POOL_MAX_IDLE_PER_URL` / `POOL_IDLE_TTL` 与 `close_pool()`。
  实测：**2 条 HTTP 请求 → 1 条 TCP 连接 → 1 条 usbmux 隧道**（此前是 2 条隧道、
  8 条 usbmuxd 连接）。设 `POOL_ENABLED=False` 可退回旧行为。
* feat: `USBClient(transport=...)`，默认 `"auto"`（优先内建转发，不可用时回退
  `http+usbmux`）；也可显式 `"forward"` / `"usbmux"`。新增 `USBClient.disconnect()`
  释放转发端口与池中连接。
* fix: `USBMuxHTTPConnection.connect()` 现在会把 `HTTPConnection.timeout` 真正作用到
  socket 上，并开启 `TCP_NODELAY`——此前超时形同虚设，WDA 卡住时读写会无限期阻塞。
* fix: 统一 `MuxError` / `MuxConnectError` 的定义。此前 `wdap.exceptions` 与
  `wdap.usbmux.exceptions` 各定义了一份同名类，而 `wdap.MuxConnectError` 指向的那份
  **运行时永远不会被抛出**，导致 `except wdap.MuxConnectError` 抓不到真实异常。
  现在顶层名字统一指向真正会抛出的那份。
* diagnose: `diagnose_wda.py` 新增"**对照组**"——用同一条中继去连 lockdown(62078)，
  把「中继通道坏了」和「目标端口没服务」彻底分开；并新增第 4 步直接测内建转发。
  两者组合可对"WDA 在跑但连不上"下确定论。
* feat: **设备激活（仅 USB）**。`wdap.lockdown` + `wdap.activation` 用纯标准库实现了
  对标 go-ios `ios activate` 的完整流程：usbmux 连 lockdown(62078) -> `StartSession`
  （双向 TLS，客户端证书取自配对记录）-> `StartService(com.apple.mobileactivationd)`
  -> `CreateTunnel1SessionInfoRequest` / `CreateActivationInfoRequest` /
  `HandleActivationInfoWithSessionRequest`，中间把 handshake 与 activation-info 转发到
  `albert.apple.com`。未嵌入 go-ios / tidevice，可安全打进 ipa。
  API：`wdap.activation_state()` / `wdap.is_device_activated()` /
  `wdap.activate_device()` / `wdap.activate_all_usb_devices()`；
  CLI：`python activate_ios.py --list|--state|--all [--proxy ...]`。
  已激活的设备调用后直接返回，可安全重复执行。
* feat: **WDA 运行日志接口 `client.log`**（对应服务端 `/wda/log/*` 的 7 个端点）。
  `recent` / `errors` / `stats` / `crash` / `download` / `save` / `clear` / `config`，
  另加 `available()` 能力探测与 `follow()` 增量跟随（按 `seq` 推进 `since` 轮询）。
  排查"端口还在但没反应""跑一小时整个 WDA 掉了"这类事后看不见的问题时用它。
  两个服务端契约已在客户端固化：
  1. 路由是 `withoutSession` + `standalone`，路径**不带** `/session/:id` 前缀
     （一律走 `client.http`），并且绕开共享路由队列——所以队列卡死时也问得到；
  2. **GET 的参数也要放在 JSON body**：服务端 `FBWebServer` 只做
     `JSONObjectWithData:request.body`，`?limit=10` 这种 query **完全不生效**。
  参数侧提前拦截服务端的静默行为：`limit` 超出 1..20000 会被 clamp、`level` 写错会被
  静默当成 `info`，本地一律抛 `ValueError`；`category` 是纯字符串，服务端允许新分类，
  故不校验。`/wda/log/download` 返回 `text/plain`，为此给 `BaseClient` 增加
  `_fetch_raw()`（不再经 `_unsafe_httpdo` 的 `response.json()`）与 `wda_url` 属性。
  路由不存在（旧 WDA）时抛 `LogUnsupportedError`，`available()` 返回 False。

0.2.3
-----

* fix: **纠正"隧道被 RST"的误判**。0.2.2 把 `ConnectionResetError(10054)` /
  `ConnectionAbortedError(10053)` 一律归为"瞬态错误"，于是一次**持续**的重置仍被当成
  "WDA 没启动"，继续徒劳地调 `tidevice xctest`（iOS 17+ 必然失败）并刷屏。
  现在改为**自证式判定**：隧道被 RST 时先探一次 usbmuxd 控制通道（`list_devices`）——
  控制通道正常 → 说明只是"设备上那个端口连不上"，值得激活；
  控制通道也不通 → 判定为 usbmux 通道级故障，直接报错、不再徒劳激活
* fix: `_explain_probe_error` 对 RST 不再谎称"瞬态错误"，改为列出真实诱因
  （端口无监听 / usbmuxd 被其它工具争用 / USB 连接不稳）
* perf: `select_device()` 结果增加短 TTL 缓存（`wdap.usbmux.DEVICE_CACHE_TTL`，默认 2s，
  设 0 关闭）。`create_mux()` 每次要连 usbmuxd 两次（版本探测 + 建会话），而
  `http_create()` 每个请求都要重新解析一遍设备，等于**每个 HTTP 请求向 usbmuxd 开 4 条连接**；
  缓存后降到 2 条，显著降低连接 churn（churn 本身是一类"跑一会就开始被 RST"的诱因）。
  连接层异常时缓存立即作废，设备重插/换口可快速恢复
* feat: `diagnose_wda.py` 升级为分层自证诊断
  * 第 0 层：检查 tidevice / ios.exe / iTunes 等是否在争用 usbmux，并查 AMDS 服务状态
  * 第 3 层拆成 3b `create_mux()` / 3c `Connect` 命令 / 3d 隧道上跑原始 HTTP，且**重复多次**
    —— "一次成功一次失败"即可判定为 churn/争用，而不是通道彻底坏掉
  * 末尾给出不依赖 usbmux 的替代路径（WiFi 直连、`ios forward`）

0.2.2
-----

* feat(cv): 新增 16 个图像接口，CV 端点从 9 个扩到 25 个
  * OpenCV: `wait_for_stable` / `match_features` / `match_templates` / `compare` /
    `preprocess` / `edges`
  * Vision: `barcode` / `rectangles` / `saliency` / `faces` / `humans` / `classify` /
    `contours` / `document` / `text_rectangles` / `align`
* fix(usbmux): **修复长跑后 `WinError 10054 远程主机强迫关闭了一个现有的连接`**。
  `usbmux.fetch()` 每次请求都会新建一条连接（`http+usbmux` 下是一条 usbmux 隧道），
  但读完响应后从 `conn.close()`，只靠 GC 回收。隧道/句柄持续累积，设备端 HTTP 服务或
  usbmuxd 达到并发上限后就会 RST 新连接。现在用 `finally` 保证任何路径都关闭连接；
  由于异常对象持有 traceback（→ 持有栈帧 → 持有 `conn`），上层若把异常存下来，
  不关闭会导致该隧道永久泄漏
* fix: `_unsafe_httpdo` 增加传输层瞬态错误自动重试。捕获
  `ConnectionResetError` / `ConnectionAbortedError` / `BrokenPipeError` /
  `TimeoutError` / `RemoteDisconnected` / `BadStatusLine` / `ResponseNotReady`
  以及 usbmux 用 `HTTPError(args[0])` 包装后的同类错误，丢弃旧连接重建后重试。
  默认只对幂等方法（GET/HEAD/OPTIONS/DELETE）重试，避免点击/输入等 POST 动作被重复执行；
  设备消失（`MuxConnectError`）与 WDA 业务错误不重试，仍按原样抛出
* feat: 新增配置开关 `HTTP_TRANSPORT_TRIES`（默认 2）、`HTTP_TRANSPORT_RETRY_DELAY`
  （默认 0.5s）、`HTTP_TRANSPORT_RETRY_ON_POST`（默认 False）
* fix: **修复"设备上 WDA 明明在跑，`USBClient()` 却判定 WDA 没启动"**。
  `is_ready()` 用 `except Exception: return False` 把所有失败原因吞掉，且只探测一次，
  usbmux 隧道首次建连慢或一次瞬时 RST 都会被误判成"WDA 没启动"；随后 `USBClient`
  又无条件调用 `tidevice xctest`——这在 iOS 17+ 必然失败
  （`DeveloperServiceError: DeveloperImage not found`），徒增噪音与 usbmuxd 压力。
  现在：
  * 新增 `BaseClient.probe()`，返回 `(ok, value_or_exception)`，不再丢原因
  * `is_ready()` 新增 `timeout` / `tries` / `delay` 参数（默认值与旧行为一致），
    可对瞬态失败重试
  * `wait_ready()` 超时结束时打印最后一次失败原因，不再只有一句 "device still offline"
  * 新增 `_explain_probe_error()`，把 usbmuxd 不可达 / 中继被拒（设备 8100 无监听）/
    设备未枚举 / 隧道被重置等区分开
  * `USBClient()` 新增 `auto_activate`（默认 True）、`activate_timeout`、
    `probe_tries`、`probe_timeout` 参数。探测失败会先记录**真实原因**；
    若是设备/usbmuxd 通道本身的问题则直接报错（启动 WDA 也救不了），
    不再做徒劳激活；报错信息含 iOS 17+ 的处置提示
  * `_start_wda_xctest()` 的子进程输出改为写入
    `%TEMP%/wdap_tidevice_xctest.log`，失败时只在日志里给一行摘要 + 日志路径，
    不再让整段 tidevice traceback 刷屏盖掉真正的报错
* feat: 新增 `diagnose_wda.py`——逐层诊断脚本，定位
  "WDA 在跑但探测失败"卡在哪一层（usbmuxd → 设备枚举 → 中继 → http+usbmux → 本机转发）

0.2.1
-----

* fix(cv): `ocr()` 移除 `index` / `tap` / `duration` / `taps` 参数——服务端
  `handleRecognizeText` 不走通用点击逻辑，传了会被静默忽略。要点文字请用 `find_text()`
* fix(cv): `region` 的 width / height 必须为正。服务端拿到零尺寸区域会直接报
  "The search region is outside of the source image bounds"，不会退化成整屏
* fix(cv): `snapshot(format="jpg")` 归一化为 `"jpeg"`——服务端只认 `jpeg`，
  写 `jpg` 会静默退回 png
* fix(cv): 本地校验 `mode` / `method` / `colorSpace` / `level` / `taps` /
  `timeout` / `interval` / `threshold` / `quality` / `minimumTextHeight` 取值范围，
  避免写错后被服务端静默 clamp 或退回默认值
* fix(cv): `CVStatus` 新增 `available` 属性；`__bool__` 改为"具备任意一项图像能力"
* fix: 补上 `wdap.__version__`（此前只存在于不打包的仓库根 `__init__.py`，
  `import wdap; wdap.__version__` 会 AttributeError）
* feat(cv): 新增 `CVUnsupportedError`。设备上的 WDA 未编译 CV 支持时，9 个端点会返回
  `status=110 Unhandled endpoint`，现翻译为带成因说明与验证方法的明确异常

0.2.0
-----

* feat(cv): 新增 `wdap/cv.py`，封装 wda_cv_vision 分支的 9 个图像/文字识别端点
  （`/wda/cv/status`、`/wda/cv/snapshot`、`/wda/vision/ocr`、`/wda/vision/findText`、
  `/wda/vision/waitForText`、`/wda/cv/matchImage`、`/wda/cv/waitForImage`、
  `/wda/cv/findColor`、`/wda/cv/waitForColor`），通过 `client.cv` 访问
* feat(cv): `CVResult` / `CVMatch` / `CVStatus` 结果封装，支持逻辑点与像素坐标互转、
  debug 标注图保存；模板图支持 文件路径 / bytes / base64 / PIL.Image 四种入参
* feat(settings): 补全 `AppiumSettings` 枚举至 34 个 key（与服务端 FBSettings.m 一一对应），
  新增 `APPIUM_SETTINGS_SPEC` 元数据表与 `validate_appium_settings()` 本地校验
* feat: `appium_settings()` 增加 `validate` 参数，避免拼错 key 被服务端静默忽略
* feat: 补齐 Session 级缺失端点（screens、launchUnattached、定位、appearance、
  rotation、tapWithNumberOfTaps、twoFingerTap、forceTouch、pressAndDrag、scroll、
  swipe、pinch、rotate、touch_id、siri、notification、appAuth、accessibilityAudit、
  video、voiceOver、keyboardInput 等）
* feat: 补齐 Element 级缺失端点（screenshot、doubleTap、twoFingerTap、forceTouch、
  rotate、swipe、pressAndDrag、drag、scrollTo、getVisibleCells、keyboardInput、
  focuse、focused 等）
* doc: README 新增 CV/Vision 用法、Appium Settings 参数表与新增端点清单

0.1.5
-----

* 首个发布版本，基于 openatx/facebook-wda，增强复杂触摸动作（TouchAction）

1.4.7
-----

* Update release.yml
* update github release (#146)
* fix: fix PR comment
* fix: fix PR comment
* fix: remove ds store file
* doc: add report link to readme
* feat: support tap api older version and add 611version test report
* feat: add some unsupport func
* feat: add some unsupport func
* doc: add v771 e2e test report
* doc: add v771 e2e test report
* feat: add all e2e benchmarks
* feat: add find element e2e testing
* feat: add find element e2e testing
* feat: add benchmark e2e testing (element and before etc.)

1.4.6
-----

* add github workflow

1.4.5
-----

* set trust\_env to False

1.4.4
-----

* set no\_proxy since some pc socket.gethostbyname is very slow
* callback example fix

1.4.3
-----

* add cahced\_property to alibaba

1.4.2
-----

* hotfix for screenshot().save(s.jpg) error

1.4.1
-----

* fix #102

1.4.0
-----

* add new api for press\_duration
* use self write AttrDict class to replace attrdict lib
* update readme

1.3.4
-----

* remove noisy log

1.3.3
-----

* support tidevice

1.3.2
-----

* limit debug log length

1.3.1
-----

* add more debug info

1.3.0
-----

* change wait() default return None when element not found, previous raise Error

1.2.7
-----

* fix again

1.2.6
-----

* fix loop call

1.2.5
-----

* wait wda ready before tins xctest

1.2.4
-----

* update get scale logic

1.2.3
-----

* add try\_first in register\_callback, optimize usage of USBClient
* python2 is no logger support, remove from traivs.yml

1.2.2
-----

* add python 3.7 and 3.8 to travis.yml
* retry for stale element error
* add d.alert.click\_exists

1.2.1
-----

* Add en button name for alert monitor
* add timeout in http request

1.2.0
-----

* fix first call windows\_size has to start com.apple.Preferences bug, add wda\_bundle\_id argument

1.1.10
------

* fix nameMatches, update tests
* fix info get error
* fix window size request twice error

1.1.9
-----

* merge WDAUnknownError
* support handle possibly crashed error

1.1.8
-----

* add retry to app\_current

1.1.7
-----

* add depth to 4
* support tins when wda is not ready

1.1.6
-----

* add \_fast\_swipe
* add 无线局域网与蜂窝网络 to watch\_and\_click

1.1.5
-----

* hotfix window\_size return (0,0) error

1.1.4
-----

* fix wait\_device and fix-session-id both occur error
* add open watch button

1.1.3
-----

* change http timeout to 180s and wait device to 180s
* add watch\_and\_click usage
* hotfix for inside platform

1.1.2
-----

* fix spell
* fix check isatty bug

1.1.1
-----

* only tmq and not local device use /mds/touchAndHold

1.1.0
-----

* add d.alert.watch\_and\_click support
* add watch and click support

1.0.12
------

* change touch to mds touch for test

1.0.11
------

* wait for wda recover when 502 bad gateway

1.0.10
------

* add Selector.click

1.0.9
-----

* change wait\_debug timeout from 30s to 120s, add element.info

1.0.8
-----

* hot fix, i do not know why

1.0.7
-----

* change tmq check time

1.0.6
-----

* add appium\_settings, fix invalid\_session\_id not handle old wda
* slow find elements

1.0.5
-----

* fix missing delete bug

1.0.4
-----

* fix selector getattr recursive error

1.0.3
-----

* show error message

1.0.2
-----

* add alert check before send\_keys, for tmq platform

1.0.1
-----

* fix element operation error

1.0.0
-----

* finish register\_callback
* update doc
* add callback

0.9.7
-----

* update session-id when session-id matched application crashed. for old wda

0.9.6
-----

* simplefy error\_callback logic, fix infinite callback error
* remove useless api
* fix ipython complete, add more tests

0.9.5
-----

* bug fix

0.9.4
-----

* reduce /status request count

0.9.3
-----

* change el.click logic to get position first and send tap request

0.9.2
-----

* fix compability with old WDA session

0.9.1
-----

* fix bug when device re-plug

0.9.0
-----

* fix urljoin in python 3.7
* add class USBClient, which support usb connection without iproxy

0.8.1
-----

* fix since attrdict doc not readed
* fix get sessionId

0.8.0
-----

* add press method, support args: home, volumeUp, volumeDown

0.7.7
-----

* compatible with old WDA

0.7.6
-----

* merge master
* fix conflict
* fix c.windows\_size() when session not created
* add golang wda client link
* update readme

0.7.5
-----

* fix alert callback missing error

0.7.4
-----

* fix compatibility for old version of wda

0.7.3
-----

* when error:invalid-session-id occurs, retry again

0.7.2
-----

* raise error when return value contains error key

0.7.1
-----

* fix session() with no argument error

0.7.0
-----

* update doc
* move all Session methods to Client

0.6.2
-----

* rename wda\_alibaba to wda\_taobao

0.6.1
-----

* add taobao property

0.6.0
-----

* add alibaba property

0.5.0
-----

* add home function to Session
* add app\_list, even useless

0.4.2
-----

* fix swipe\_up-down-left-right, close #89

0.4.1
-----

* fix python2 support, close #76
* update doc

0.4.0
-----

* update readme
* add more functions
* add method s.xpath(...)
* make httpdo thread safe

0.3.9
-----

* fix app launch stuck, set Quiescence to False

0.3.8
-----

* check url format when create Client

0.3.7
-----

* fix compatible for the latest modified WDA by appium

0.3.6
-----

* fix WDARequestError, format code with yapf

0.3.5
-----

* add pillow depencency

0.3.4
-----

* fix: #65

0.3.3
-----

* fix all the tests

0.3.2
-----

* compatiable with py2

0.3.1
-----

* add retry for status, since status sometime get EmptyReponse

0.3.0
-----

* make session launch more robust
* change json.decoder.JSONDecodeError to WDARequestError

0.2.4
-----

* fix session, can not launch error

0.2.3
-----

* add content-type application/json when send http post
* recommend using appium/WebDriverAgent
* fix set\_callback check error

0.2.2
-----

* fix travis
* try catch response.json()
* add wait\_ready function
* add screenshot in session, add click(support percent), add scale, update readme
* [issue #33]Add escape character for quotes

0.2.1
-----

* fix in py3.6 error
* fix name matches, use string escape fix it
* add test example
* fix nameMatches
* add netease cloudmusic test example

0.2.0
-----

* finish first version of sync new code
* fix some tests
* add nameMatches
* sync to lastest wda
* sync to new wda code
* Allow extra capabilities to be passed to the session (#25)
* [Screenshot] Open file in binary mode

0.1.2
-----

* freeze webdriveragent version
* change example tab to 4space
* support handle alert automaticly
* Sessions with arguments (#23)
* change class\_name to className and text\_contains to textContains

0.1.1
-----

* Lukzeg change orientation functionality (#21)
* add Element change lot of code
* add some beta apis
* follow format
* Change time\_out to timeout and add example usage in readme
* add alert.wait and Selector partial text
* support get current session id and bundle id

0.1.0
-----

* add pinch api
* update doc
* update accessible,pinch,keyboardDismiss,doubleTap,deactivateApp
* close #15
* update api sync with WDA (#12)
* add healthcheck api
* fix broken swipe() API, due to wda specific endpoint complied with WD spec naming convention https://github.com/facebook/WebDriverAgent/commit/eeaa1a3bbc9559d41ff3779c73c6678c7715a3d0
* fix unicode convert to unicode error
* fix missing e args
* support python3

0.0.3
-----

* retry request when first failed
* Update \_\_init\_\_.py
* Update \_\_init\_\_.py
* update README.md file, adding example for element swipe api
* Add swipe support for wda element
* add comment and fix some code structure according to the pr
* expand ‘link text’ query of Selector class with ‘value’ or ‘label’ property, in case no ‘name’ property exists and only ‘value’ property valid
* fix the right api
* add send\_keys, but it not works actually
* sync to the latest wda
* status func add sessionId
* just add some comment
* add alert support, close #6

0.0.2
-----

* raise exceptions when connect went wrong
* add more about it
* add more ..
* change rect to bounds, add DEBUG mode
* remove extra code
* finish scroll part
* add scroll function
* limit max find element time into 90s
* merge code
* add bounds
* Add attribute
* Add getAttribut()
* Update \_\_init\_\_.py
* Fix find element when element's isvisible=NO
* add tap\_hold api
* enable swipe function
* Fix xpath定位
* Add xpath 定位
* Update README.md
* Update README.md
* Update README.md
* Update README.md
* Update README.md
* fix documentation, add some props
* do some fix
* update desc

0.0.1
-----

* wow, update again, this is the last today
* update document
* add the rest api
* fix json
* make doc looks beautiful
* add with operation
* fix travis setting
* update password
* add travis badge
* add travis
* add screenshot and selector
* add selector
* add tap and session close
* add session, and home function, wrap python requests functions
* update but not implement except status
* initial first release
* init project
* Initial commit
